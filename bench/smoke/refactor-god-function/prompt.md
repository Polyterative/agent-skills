Refactor report.py: the `process` function does too many things. Split it into small, single-purpose functions (parsing, validation, aggregation, formatting) without changing observable behavior. `python3 test_report.py` must still pass, `process` must keep its exact signature and remain the public entry point, and no function in report.py may be longer than 15 lines. Do not modify test_report.py.
